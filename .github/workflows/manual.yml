name: Saying Hello
on:
  workflow_dispatch:
    inputs:
      name:
        description: "Your name"
        required: true
        type: string
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run:  echo "Hello, ${{ github.actor }}!"